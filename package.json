{
  "name": "ai-hotspot-radar",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "api:dev": "uvicorn apps.api.app.main:app --reload --host 0.0.0.0 --port 8000",
    "web:dev": "npm --prefix apps/web run dev",
    "db:reset": "docker compose down -v && docker compose up -d --build",
    "docker:up": "test -f infra/env/.env || cp infra/env/.env.example infra/env/.env && docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  }
}
